spring.datasource.url=${DB_URL:jdbc:oracle:thin:@//localhost:1522/XEPDB1}
spring.datasource.username=${DB_USERNAME:autotrade}
spring.datasource.password=${DB_PASSWORD:}

# Broker adapter base URLs (E4) will be added here once adapters exist:
# Alpaca paper trading endpoint, Binance testnet endpoint.
