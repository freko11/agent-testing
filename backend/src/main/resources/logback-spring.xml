<?xml version="1.0" encoding="UTF-8"?>
<!--
  E7-F1-S1: one consistent log format across every profile (local/paper/prod) and every
  backend service — console-only, since stdout is already what ./mvnw spring-boot:run and
  the packaged jar's process supervisor capture. Not per-profile: the AC asks for a
  consistent format, not an environment-specific one.
-->
<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX} %-5level [%thread] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
